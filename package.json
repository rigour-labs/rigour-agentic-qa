{
  "name": "@rigour-labs/qa-monorepo",
  "version": "0.1.0",
  "private": true,
  "description": "Agentic QA framework — write scenes, get autonomous tests",
  "repository": {
    "type": "git",
    "url": "https://github.com/rigour-labs/rigour-qa.git"
  },
  "author": "Rigour Labs <hello@rigour.run>",
  "license": "MIT",
  "workspaces": [
    "packages/sdk-node"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "clean": "pnpm -r exec rm -rf dist node_modules"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
